[build]
builder = "NIXPACKS"
buildCommand = "cd health_monitor/backend && pip install --upgrade pip && pip install -r requirements.txt"

[deploy]
startCommand = "cd health_monitor/backend && python backend_api.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Core Dependencies
python-dotenv==1.0.0

# API & Authentication
fastapi==0.115.0
uvicorn==0.30.6
pydantic[email]==2.9.2
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Database
supabase==2.4.2

# Utilities
numpy>=1.24.0
requests>=2.31.0
boto3>=1.28.0

# ML Models for Audio & Image Processing  
# Note: These packages are heavy (>2GB) and may exceed Render free tier limits
# Uncomment only if deploying to paid Render plan or AWS/GCP
# openai-whisper
# deepface
# tensorflow>=2.13.0
# librosa>=0.10.0
# soundfile>=0.12.0
# opencv-python-headless>=4.8.0
# Pillow>=10.0.0

# For now, text-only mode uses AWS Bedrock (no heavy ML dependencies)

# Prevent Rust build issues on Render
--only-binary :all:

# Core Dependencies
python-dotenv==1.0.0

# API & Authentication
fastapi==0.115.0
uvicorn==0.30.6
pydantic[email]==2.9.2
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Database
supabase==2.4.2

# Utilities
numpy>=1.24.0
requests>=2.31.0
boto3>=1.28.0

# Lightweight open-source models for audio/image processing
# Small models that work on Render free tier (~100MB total)
SpeechRecognition>=3.10.0  # Audio transcription framework
vosk>=0.3.45  # Lightweight offline speech recognition (~50MB)
pydub>=0.25.1  # Audio file handling
fer>=22.5.1  # Facial Emotion Recognition (~30MB)
opencv-python-headless>=4.8.0  # Required by FER
Pillow>=10.0.0  # Image handling
mtcnn>=0.1.1  # Face detection for FER

# Prevent Rust build issues on Render
--only-binary :all:

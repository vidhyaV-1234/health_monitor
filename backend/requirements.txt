# Core Dependencies
python-dotenv==1.0.0

# API & Authentication
fastapi==0.115.0
uvicorn==0.30.6
pydantic[email]==2.9.2
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Database
supabase==2.4.2

# Utilities
numpy>=1.24.0
requests>=2.31.0
boto3>=1.28.0

# Ultra-lightweight models for audio/image processing
# Optimized for Render free tier (512MB RAM limit)
SpeechRecognition>=3.10.0  # Audio transcription (uses Google API - free, 0MB)
pydub>=0.25.1  # Audio file handling
deepface>=0.0.79  # Emotion detection (~5MB with opencv backend)
opencv-python-headless>=4.8.0  # Required by DeepFace (lightweight)
Pillow>=10.0.0  # Image handling
tf-keras>=2.16.0  # Lightweight Keras (required by DeepFace)

# Google Calendar API
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
google-api-python-client>=2.100.0

# Firebase for Push Notifications
firebase-admin>=6.2.0

# Prevent Rust build issues on Render
--only-binary :all:
